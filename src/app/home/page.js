// Now that our stylesheets are set up, we need to create our pages’ components.
// Starting with LandingPage, just like with our header,
// we need to export the component in javascript path=src/app/home/page.js

`use client`;

export default function LandingPage() {
  return <div>LANDING PAGE</div>;
}
// And we will add this into our root page
